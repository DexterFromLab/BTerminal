# BTerminal Global Default Rules
# Maintained by the BTerminal author. Updated via git pull.
# Lines starting with # are comments (disabled).
# To disable a rule: prefix it with #
# Changes take effect on the next rules injection.

Nie commituj ani nie pushuj kodu bez wyraźnej prośby użytkownika.
Przed użyciem `consult`, sprawdź dostępne modele: `consult models`. Używaj pełnych ID z prefixem providera, np. `google/gemini-2.5-pro` — nigdy nie skracaj.
Przy wykryciu błędu najpierw zidentyfikuj root cause (git log, grep, czytanie kodu) zanim zaproponujesz fix.
Zawsze używaj w pierwszej kolejności narzędzi wbudowanych w BTerminal.
Gdy użytkownik kilkukrotnie koryguje Cię w ten sam sposób — uruchom /reflect zamiast kontynuować.
Przed wykonaniem zadania, sprawdź dostępne skille.
